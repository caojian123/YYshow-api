<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yyw.myblog.mapper.UserDao">
    <!--  User 类 -->
    <resultMap id="queryResultMap" type="com.yyw.test.domain.User">
		<result column="username" property="username" jdbcType="VARCHAR" />
		<result column="id" property="userId" jdbcType="INTEGER" />
		<result column="loginTime" property="loginTime" jdbcType="TIMESTAMP" />
	</resultMap>
	
	<select id="getUserById" resultMap="queryResultMap" parameterType="java.lang.Integer">
	   SELECT *
	   FROM user
	   WHERE id = #{userId} 
	   LIMIT 0,1 
	</select>
	
	<select id="login" resultMap="queryResultMap" parameterType="java.lang.Object">
	   SELECT *
	   FROM user
	   WHERE username = #{username,jdbcType=VARCHAR}
	   AND   password = #{password,jdbcType=VARCHAR} 
	   LIMIT 0,1 
	</select>
	
	<update id="updateUser" parameterType="java.lang.Object">
	   UPDATE user
	   SET username = #{username,jdbcType=VARCHAR},
	       password = #{password,jdbcType=VARCHAR} 
	   WHERE id = #{userId}
	</update>
</mapper>